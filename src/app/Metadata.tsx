// Metadata.tsx
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Nourify",
  description: "Where Healthy Choices Meet Peak Freshness",
};

export default function MetadataComponent() {
  return null; // This component does not render anything; it only sets metadata.
}
